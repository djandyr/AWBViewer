'use strict';

describe('myApp.preview module', function() {

  beforeEach(module('myApp.preview'));

  describe('preview controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var previewCtrl = $controller('PreviewCtrl');
      expect(previewCtrl).toBeDefined();
    }));

  });
});