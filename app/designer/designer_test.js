'use strict';

describe('myApp.designer module', function() {

  beforeEach(module('myApp.designer'));

  describe('designer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var designerCtrl = $controller('DesignerCtrl');
      expect(designerCtrl).toBeDefined();
    }));

  });
});